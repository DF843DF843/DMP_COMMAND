Fix version tag showing raw error JSON (status 404 body): a failed GET_PowerAppVersion_Content (404, file/path not found) still returns a non-null error body, which the previous coalesce() wrongly accepted as valid content. CMP_PowerAppVersionResolved now explicitly checks the GET action's runtime status ('Succeeded') before using its body, falling back to the existing PowerAppVersionText otherwise. Agent 4 -> [1.2.4]. Also fixed inconsistent KPI font sizes (Critical was 38/30 while Warnings/Agents were still 34/28 from an earlier partial edit) - unified all three to 40/32 value + 19/13 label, and pushed the whole KPI block down to align its bottom edge with the left column's status line (bottom margin ~3px, matching user feedback that spacing from the bottom edge was still too generous). Bump PowerApp_Version.txt to v1.3.9, Solution -> 7.8.20

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
